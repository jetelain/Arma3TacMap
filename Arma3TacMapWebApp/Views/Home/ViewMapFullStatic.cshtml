@model StaticMapModel
@{
    Layout = "_LayoutFull";
}

@{await Html.RenderPartialAsync("_StaticMap", Model);}

@section Scripts {
    @{await Html.RenderPartialAsync("_MapScripts", Model);}
}
@section Styles {
    @{await Html.RenderPartialAsync("_MapStyles", Model);}
}
